# v0.1.1

README and install experience. Skill body unchanged.

## Changed

- README rewrite: centered hero + logo (light/dark) + failure-mode trio + philosophy + collapsible layout
- Bilingual run-loop diagram: English by default, each README gets its own + Pages interactive versions
- Three-way install: `npx skills` (verified) + per-harness plugin matrix + manual fallback
- More adapters: Cursor / Devin / Kimi plugins, Gemini extension, pi-package
