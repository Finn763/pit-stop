# v0.1.2

[中文 →](v0.1.2-zh-CN.md)

First self-bootstrap run: pit-stop improving pit-stop. Skill body and guardrail fixes.

## Changed

- hooks/block-destructive.sh rewritten: dropped the jq/grep dependency (missing tools
  silently passed everything), now pure bash + sed; fails closed when the command can't
  be parsed; `rm` blocked only in command position (incl. sudo/do/command prefixes, so
  `man rm`/`grep rm` pass), plus `git rm` and `find . -delete`
- README (EN/ZH): removed two fake install claims (Cursor `/add-plugin`, OpenCode skill
  auto-discovery), replaced with real mechanisms (Cursor `.cursor/rules/`, OpenCode
  command entry), dead badge anchor removed, repo layout lists all adapters
- SPEC header v2 → v3 (date synced), §7 layout synced with reality, §4 L2 hook
  description aligned with implementation
- guardrails.md L2 hook description aligned (command-position rm, git rm, find -delete,
  fail-closed)
- GEMINI.md: 4-line stub → full condensed protocol (parity with Cursor rule)
- SKILL.md argument-hint gains `--max-rounds N`; entry points unified on `--禁区`
- Adapter/package versions 0.1.0 → 0.1.2

## Process

Full five-phase self-run: load → find (claims audit) → fix → independent adversarial
review (caught a new fake claim in the Cursor row + overly broad hook matching) →
re-fix → re-verify. Hook matrix 24/24 (12 blocked, 12 allowed, + fail-closed).
