# v0.1.2

[English →](v0.1.2-en.md)

自举第一跑：用 pit-stop 改进 pit-stop 自己。skill 本体与护栏修复。

## 改了什么

- hooks/block-destructive.sh 重写：去掉 jq/grep 依赖（装不上会静默放行一切命令），
  改纯 bash+sed；命令解析不出就拦（fail-closed）；rm 拦截改为命令位匹配
  （含 sudo/do/command 前缀，不误伤 `man rm`/`grep rm` 等），新增 `git rm`、`find . -delete`
- README 中英两版：删两个假安装声明（Cursor `/add-plugin`、OpenCode skill 自动发现），
  改走真实机制（Cursor `.cursor/rules/`、OpenCode command 入口），badge 死锚点移除，
  仓库结构补全全部适配目录
- SPEC 头 v2→v3（日期同步），§7 仓库布局与仓库现实同步，§4 L2 钩子描述与实现对齐
- guardrails.md L2 钩子描述与实现对齐（rm 命令位、git rm、find -delete、fail-closed）
- GEMINI.md 从 4 行 stub 扩成完整压缩协议（对齐 Cursor 规则）
- SKILL.md argument-hint 补 `--max-rounds N`；.opencode/commands 入口 `--forbidden` 统一 `--禁区`
- 各端 plugin/package 版本号 0.1.0 → 0.1.2

## 过程

自举跑完五阶段：装载 → 找缺点（claims audit）→ 修 → 独立 reviewer 对抗审查
（发现 Cursor 行新不实声明 + hook 误拦面过大）→ 回修 → 再验证。
hook 全矩阵 24/24（12 拦 12 放 + fail-closed）。
