description = "Run a full pit-stop on a project: read, find, fix, verify, report"
prompt = "Run the pit-stop skill on {{args}} (project path, optional --禁区 zones). Five phases in order: load, find (path:line evidence, tagged, strength badged), propose, fix (review loop, max 3 rounds), report (changed/verified/unverified/remaining). Guardrails: banned list enforced, push/commit never automatic, evidence before claims, secrets redacted."
